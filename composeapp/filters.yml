match_attrs.coffee: |
  (doc, req) ->
    {_} = require 'underscore'
    data = req.query.tests
    data = JSON.parse data

    keys = _.keys data
    doc = _pick doc, keys
    return _.equals doc, data
