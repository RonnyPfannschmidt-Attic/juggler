match_attrs.coffee: |
  (doc, req) ->
    data = req.query.tests
    data = JSON.parse data
    checkitem = (item) ->
      for key, value of item
        if doc[key] != value
          return false
      return true

    for item in data
      if checkitem(item)
        return yes
    return no
